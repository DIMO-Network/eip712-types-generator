// Code generated by github.com/DIMO-Network/eip712-types-generator DO NOT EDIT.
package {{ .Package }}

import (
	"math/big"

	"github.com/ethereum/go-ethereum/common"
	"github.com/ethereum/go-ethereum/signer/core/apitypes"
)

{{range .Types}}
// {{.Name}} represents the EIP-712 type {{.Name}}({{range $index, $element := .Members}}{{if $index}}, {{end}}{{$element.Type}} {{$element.Name}}{{end}}).
type {{.Name}} struct {
	{{- range .Members}}
	{{.GoName}} {{.GoType}} `json:"{{.Name}}"`
	{{- end}}
}

func (t *{{.Name}}) Name() string {
	return "{{.Name}}"
}

func (t *{{.Name}}) Type() []apitypes.Type {
	return []apitypes.Type{
		{{- range .Members}}
		{Name: "{{.Name}}",	Type: "{{.Type}}"},
		{{- end}}
	}
}

func (t *{{ .Name}}) Message() apitypes.TypedDataMessage {
	return apitypes.TypedDataMessage{
		{{- range .Members}}
		"{{.Name}}": t.{{.GoName}},
		{{- end}}
	}
}
{{end}}
